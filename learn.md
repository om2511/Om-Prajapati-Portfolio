Codex [2026-04-02 04:53:53 UTC] Git branch creation and local server binding can be blocked by the sandbox even when filesystem edits in the workspace are allowed. For this project, Git writes required escalated execution and backend runtime validation inside the sandbox was limited by `EPERM` on port binding, so future validation steps should separate code correctness from environment restrictions and record that distinction explicitly.
Codex [2026-04-02 12:00:00 UTC] Hero animation text with variable phrase length must be isolated from the main heading layout. Timing changes alone do not solve reflow. The stable fix is a dedicated fixed-height shell outside the heading so the animation cannot move the rest of the page.
Codex [2026-04-02 12:00:00 UTC] Navbar active-state styling is only as reliable as the section-tracking logic behind it. A single low-threshold intersection callback is not enough for mid-page sections, so the robust fix is to combine observer state with a scroll-position fallback tied to section offsets.
Codex [2026-04-02 12:00:00 UTC] One-time reveal observers break when later UI state adds new `.reveal` nodes. If a section can append content after initial mount, those nodes need explicit visibility handling or a reusable observer lifecycle.
